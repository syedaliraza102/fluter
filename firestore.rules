rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /products/{productId} {
      // Development: open access. Replace with auth checks for production.
      allow read, write: if true;
    }
  }
}
